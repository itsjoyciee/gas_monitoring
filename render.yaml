services:
  - type: web
    name: gas_monitoring
    env: php
    buildCommand: composer install --no-dev --optimize-autoloader
    startCommand: /usr/bin/php -S 0.0.0.0:8000 -t public
    envVars:
      - key: APP_ENV
        value: production
    staticPublishPath: ./public
