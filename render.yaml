# render.yaml
services:
  - type: web
    name: gas_monitoring
    env: php
    buildCommand: composer install --no-dev  # If using Composer
    startCommand: php -S 0.0.0.0:8000 -t public  # Serve from `public/`
    staticPublishPath: ./public  # Point to your web root
